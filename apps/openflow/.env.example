# App
NEXT_PUBLIC_APP_URL=http://localhost:4168
APP_NAME=OpenFlow
NODE_ENV=development

# Database
DATABASE_URL=file:./data/openflow.db

# Auth
AUTH_SECRET=your-secret-here-min-32-chars

# Logto OIDC (optional — uses mock auth without these)
LOGTO_ISSUER=
LOGTO_CLIENT_ID=
LOGTO_CLIENT_SECRET=

# AI Icon Generation (optional — uses fallback shapes without key)
ANTHROPIC_API_KEY=
