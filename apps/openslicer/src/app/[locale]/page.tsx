import { SlicerLayout } from "../../components/layout/SlicerLayout";

export const dynamic = "force-dynamic";

// This is the main slicer page — renders the full-viewport 3D slicer
export default function SlicerPage() {
  return <SlicerLayout />;
}
